<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-2">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <ul class="nav navbar-nav navbar-brand">
        <% if current_page? root_path %>
        techAtlanta
        <% else %>
        <%= link_to 'techAtlanta', articles_path %>
        <% end %>
      </ul>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-2">
      <ul class="nav navbar-nav navbar-right">
        <% if signed_in? %>
          <li><%= link_to 'Edit Your Info', edit_user_path(current_user) %></li>
          <li><%= link_to 'Sign Out', signout_path, method: 'delete' %></li>
        <% else %>
          <li><%= link_to "Sign Up", root_path if current_page? signin_path %></li>
          <li><%= link_to "Sign In", signin_path if current_page? root_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>